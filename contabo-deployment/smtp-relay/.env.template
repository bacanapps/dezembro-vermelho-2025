# SMTP Relay Configuration
# Copy this file to .env and fill in the values

# Server Configuration
PORT=3000

# Internal SMTP Server (via VPN)
SMTP_HOST=10.101.40.1
SMTP_PORT=25
SMTP_SECURE=false

# SMTP Authentication (if required by government server)
SMTP_REQUIRE_AUTH=false
SMTP_USER=producao@aids.gov.br
SMTP_PASS=

# Security
# Generate a strong API key: openssl rand -hex 32
API_KEY=CHANGE_THIS_API_KEY

# Default sender
DEFAULT_FROM=Dezembro Vermelho 2025 <producao@aids.gov.br>
